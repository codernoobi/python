"""
basic
datatypes: float, double,
bool, str, int...

list
single name to collection of values
list of lists, diff data types...
index: +ve, -ve
slicing: range
var=[a,b,c]: var holds the reference to the list
var1=var : actual ref is copied so change in one reflects in other
var1=list(var) : new list is formed so change to one is not reflected in other

functions
piece of reusable code
max()		round()		type()
...
built in functions
index()			count()		append()
capitalize()	replace()
......

packages
directory of py scripts called module
NUmpy- work with arrays
matplotlid- data visulization
scikit-learn- ML

install packages using pip in cmd
import packages

"""